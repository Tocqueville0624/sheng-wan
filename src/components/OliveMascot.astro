<svg
  class="olive-mascot"
  viewBox="0 0 180 180"
  role="img"
  aria-label="An olive character holding a small financial chart"
>
  <path class="leaf" d="M87 41c6-22 23-33 45-33-1 22-15 39-40 42"></path>
  <path class="stem" d="M91 48c3-15 13-25 27-31"></path>
  <ellipse class="olive" cx="78" cy="91" rx="51" ry="63" transform="rotate(-9 78 91)"></ellipse>
  <circle class="eye" cx="61" cy="79" r="3.5"></circle>
  <circle class="eye" cx="91" cy="75" r="3.5"></circle>
  <path class="smile" d="M67 98c8 7 18 6 25-2"></path>
  <path class="arm" d="M108 103c10 3 17 10 21 21M49 109c-7 8-9 17-6 27"></path>
  <rect class="scroll" x="96" y="112" width="63" height="45" rx="7" transform="rotate(4 96 112)"
  ></rect>
  <path class="chart" d="m108 144 10-10 10 4 16-17M108 146h39"></path>
</svg>

<style>
  .olive-mascot {
    width: min(11rem, 34vw);
    overflow: visible;
  }
  .olive {
    fill: var(--olive);
    stroke: var(--ink);
    stroke-width: 3;
  }
  .leaf {
    fill: var(--olive-light);
    stroke: var(--ink);
    stroke-width: 3;
    transform-origin: 92px 48px;
    transition: transform 180ms ease;
  }
  .stem,
  .smile,
  .arm {
    fill: none;
    stroke: var(--ink);
    stroke-linecap: round;
    stroke-width: 3;
  }
  .eye {
    fill: var(--ink);
  }
  .scroll {
    fill: var(--surface);
    stroke: var(--ink);
    stroke-width: 2.5;
  }
  .chart {
    fill: none;
    stroke: var(--accent);
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 3;
  }
  .olive-mascot:hover .leaf {
    transform: rotate(-5deg);
  }
  @media (prefers-reduced-motion: reduce) {
    .leaf {
      transition: none;
    }
  }
</style>
